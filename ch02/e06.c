#include <stdio.h>
int main(void)
{
	int tose, twice, square;

	tose = 10;
	twice = tose * 2;
	square = tose * tose;
	printf("tose: %d, tose X 2: %d, tose X tose: %d\n", tose, twice, square);

	return 0;
}
